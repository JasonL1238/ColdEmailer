# Floors match the versions this app is actually tested against. The previous
# exact pins had drifted years behind the working environment and no longer
# resolved together, so a fresh `pip install -r` produced a broken install.

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.9

# MX lookups for send-time deliverability. Without it contact_verify falls
# back to a plain A-record resolve, which cannot see MX at all — so the check
# silently stops being the check its name promises.
dnspython>=2.6

# --- scraping ---
beautifulsoup4>=4.12
lxml>=5.0
httpx>=0.27
requests>=2.32               # still used by contact_enrich / google auth helpers
trafilatura>=2.0
ddgs>=9.0                    # duckduckgo-search is deprecated and returns 0 results
playwright>=1.45             # optional at runtime; used only for JS-shell pages

# --- LLM providers (only one key is needed at runtime) ---
# google-generativeai is end-of-life and prints a deprecation warning on
# import; google-genai is its supported replacement.
google-genai>=1.0
openai>=1.40                 # also used for OpenRouter (OpenAI-compatible API)

# --- Gmail ---
google-api-python-client>=2.150
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# --- resumes ---
pypdf>=4.0

# --- misc ---
python-dotenv>=1.0

# --- tests ---
# The async routes are exercised with asyncio.run(), not pytest-asyncio's
# marker, so that plugin is not a dependency — the suite passes with it
# disabled (-p no:asyncio).
pytest>=8.0
pytest-cov>=5.0
